# Upstage OCR API
UPSTAGE_API_KEY=your_upstage_api_key_here

# Neon PostgreSQL — Vercel 연동 시 자동 주입, 직접 사용 시 DATABASE_URL에 입력
POSTGRES_URL=
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Vercel Blob — Vercel 대시보드에서 Blob Store 생성 후 자동 주입
BLOB_READ_WRITE_TOKEN=
