# Upstage OCR API
UPSTAGE_API_KEY=your_upstage_api_key_here

# Vercel KV (Upstash Redis) — Vercel 대시보드에서 KV Store 생성 후 자동 주입
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Vercel Blob — Vercel 대시보드에서 Blob Store 생성 후 자동 주입
BLOB_READ_WRITE_TOKEN=
